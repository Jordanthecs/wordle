# wordle
Recreated wordle where I have a 5x5 matrix where each row is for one word input and the program compares the string to a random word from a text file. Each block changes color depending on whether the location of the character is the same as the random word.
